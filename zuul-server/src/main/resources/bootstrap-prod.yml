# 如果修改了默认服务器配置，则客户端就不能在application配置文件中配置spring.cloud.config.uri
# 必须在bootstrap 中配置。
spring:
  cloud:
    config:
      uri: http://192.168.50.49:8888
      name: zuulserver
      label: master
      profile: prod